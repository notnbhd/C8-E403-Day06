# Dependencies riêng cho agent workflow (để KHÔNG đụng requirements.txt chung).
#   pip install -r agent/requirements.txt
langgraph>=0.2.20
langchain-core>=0.3
langchain-openai>=0.2        # client OpenAI-compatible -> trỏ OpenRouter
fastapi>=0.110               # chỉ cần nếu chạy agent/api.py
uvicorn>=0.30
pydantic>=2
python-dotenv>=1.0
# Production memory (tuỳ chọn) — checkpointer trỏ Supabase Postgres:
# langgraph-checkpoint-postgres>=2.0
pytest>=8                    # dev
